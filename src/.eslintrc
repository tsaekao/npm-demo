////// src ESLint Configuration

{
  "extends": ["../.eslintrc"],
  "env": {
    // don't assume any environment since this library is isomorphic
    "browser": false,
    "node": false
  },
  "globals": {
    // safe assumptions

    // console is a valid global, but no-console in base settings will check against it
    "console": true
  }
}
